{
  "name": "1",
  "version": "1.0.0",
  "description": "Rewrite the app.js into JSX. Load it into an HTML file with a div with an id of 'root'. Use the babel repl to check your answer.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel input.js -o output.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1"
  }
}
